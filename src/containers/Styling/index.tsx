import React from 'react';

import Flexbox from './Flexbox';

const Styling: React.FC = () => {
  return (
    <React.Fragment>
      <Flexbox />
    </React.Fragment>
  );
};

export default Styling;
